#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>

using namespace std;

int main() {
    int n,k,c;
    cin >> n >> k;
    queue<int> q;
    q.push(n);
    while(!q.empty()){
        c = q.front();
        q.pop();
        
    }
}